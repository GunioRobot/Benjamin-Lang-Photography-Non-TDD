<!DOCTYPE html>
<html>
<head>
  <title>Benjamin Lang</title>
  <%= stylesheet_link_tag "layout", "c1" %>
  <%= csrf_meta_tag %>
</head>
<body>
	<div id="body_wrap">
		<div id="logo_container">
			<span class="black_bkg" id="logo_text">benjamin<span class="white_text">lang</span>photography</span>
		</div>
		<div id="top_nav_container" class="nav_container">
			<ul id="top_nav_list">
			<% @PT.each do |project_type| %>
				<li>
					<%= link_to project_type.name, "#", :class => 'nav_a' %>
				</li>
			<% end %>
			</ul>
		</div>
		<div id="content_wrap">
			<%= yield %>
		</div>
	</div>
</body>
</html>
