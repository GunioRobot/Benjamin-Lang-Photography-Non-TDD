<div id="main_content_container">
	<p>
		There aren't any photos in <em><%= @proj.name %></em> yet.
	</p>
</div>
<%= render :partial => 'shared/side_nav', :locals => { :projs => @proj.project_type.projects, :current_proj_id => @proj.id } %>